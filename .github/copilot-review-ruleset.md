# Copilot = só revisão

Settings → Rules → New ruleset → default branch.
Enable Copilot code review. Review new pushes = ligado. Effort: Balanced.
Desligar: coding agent, assign to issue, Corrigir com Copilot, auto-merge, MCP na revisão.
